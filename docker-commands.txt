docker build -f Docker\DOCKERFILE -t untangle .
//Use this command for file sharing (meaning that all changes to files in the docker image will also change the files in your current system for that folder and vice versa)
docker run -d -it -p 8080:3000 --name untangled -v /path/to/project:/var/www untangle
//Without file sharing
docker run -d -it -p 8080:3000 --name untangled untangle
//Inside the project folder run
docker cp . untangled:/var/www
//Run the app within the container
docker exec untangled /bin/bash -c "./docker-run.sh (java-version)" fx docker exec untangled /bin/bash -c "./docker-run.sh 15"